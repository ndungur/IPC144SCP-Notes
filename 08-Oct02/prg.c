#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>


int main(void) {
  int val;
  int input;

  if (input > 10) {
    val = 1;
  }
  else {
    val = 2;
  }

  val = input > 10 ? 1 : 1;

  // value = condition ? trueValue : FalseValue;

}

